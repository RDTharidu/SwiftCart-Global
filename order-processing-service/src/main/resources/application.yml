server:
  port: 8083

spring:
  application:
    name: order-processing-service
  datasource:
    url: jdbc:mysql://localhost:3306/order_processing_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
  # 🚀 EXTRA FEATURE: Email Configuration
  mail:
    host: smtp.gmail.com
    port: 587
    username: your-email@gmail.com
    password: dummy-app-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true